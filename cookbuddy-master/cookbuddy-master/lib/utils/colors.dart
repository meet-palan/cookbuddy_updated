import 'package:flutter/material.dart';

class AppColors {
  // Background Color
  static const Color background = Color.fromRGBO(244, 228, 205, 1);

  // Primary Theme Color
  static const Color primary = Colors.orangeAccent;

  // AppBar Color
  static const Color appBar = Color.fromRGBO(254, 150, 53, 1);

  // Button Colors
  static const Color buttonBackground = Color.fromRGBO(254, 150, 53, 1);
  static const Color buttonText = Colors.white;

  // Text Colors
  static const Color headingText = Color(0xFF5D4037); // Dark Brown
  static const Color bodyText = Colors.black87;
  static const Color hintText = Color(0xFF8D6E63);

  // Form Field Colors
  static const Color enabledBorder = Color(0xFFBCAAA4); // Light Brown
  static const Color focusedBorder = Colors.orangeAccent;
  static const Color errorBorder = Color(0xFFD32F2F); // Deep Red for errors

  // Icon Colors
  static const Color iconActive = Colors.orangeAccent;
  static const Color iconInactive = Colors.grey;

  // Snackbar & Validation Message
  static const Color errorText = Color(0xFFD32F2F);
}
